<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cansukose.dersbndng">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="dersbndng"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dersbndng">
        <activity
            android:name="com.cansukose.dersbndng.PremiumActivity"
            android:exported="false" />
        <activity
            android:name="com.cansukose.dersbndng.StartActivity"
            android:exported="false" />
        <activity
            android:name="com.cansukose.dersbndng.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>